

## Replace Hero Video

Swap the existing `public/hero-video.mov` with the newly uploaded `Ceramic_method_short-2.mov`. No code changes needed — the `<video>` element in `Index.tsx` already references `/hero-video.mov`.

**Single step:** Copy `user-uploads://Ceramic_method_short-2.mov` → `public/hero-video.mov` (overwrite).

